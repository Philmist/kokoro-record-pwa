# 仕様書（v1）

「起きたこと」と、それに伴う考え・行動・身体感覚・感情を記録する個人向け PWA。
CBT のコラム法に似ているが異なる独自フォーマット。記録は **担当医に見せる（印刷して渡す）** ことを主目的の一つとする。

## アーキテクチャ方針

- **完全ローカル**：サーバ・認証・外部 DB なし。データは端末の **IndexedDB**（オリジン単位で永続）に保存する。
- **Cloudflare Pages は静的ホスティングのみ**（Pages Functions は使わない）。
- 端末間同期はしない。エクスポート/インポートは v1 では実装しない（ただしデータモデルは JSON で完結し、後から出力しやすい形にしておく）。
- データ消失リスク（ブラウザのサイトデータ消去・iOS のストレージ削除・端末故障）は許容。将来のバックアップ実装余地を残す。

### 技術スタック

- Vite + Preact + TypeScript（既存足場）
- ルーティング：`preact-iso`（**History API ルーティング**。`preact-iso` はハッシュ非対応のため）。Cloudflare Pages には `public/_redirects` に `/* /index.html 200` の静的1行を置き SPA フォールバックとする（Functions 不使用）。オフライン時の深いリンクは vite-plugin-pwa の `navigateFallback` が処理
- 永続化：`idb`（IndexedDB の薄い Promise ラッパ）。アクセスは `src/db/` に隔離する
- スタイル：素 CSS + CSS カスタムプロパティ（モバイルファースト、`prefers-color-scheme` でダーク対応）
- PWA：`vite-plugin-pwa`（`registerType: 'prompt'`、`PWABadge` を流用）

## データモデル

### Entry（記録1件）

| フィールド | 型 | 必須 | 印刷 | 備考 |
|---|---|---|---|---|
| `id` | `string` | ✓ | - | `crypto.randomUUID()` |
| `date` | `string` | ✓ | ✓ | 出来事が起きた日時。`YYYY-MM-DDTHH:mm`（分単位・ローカル時刻）。初期値 now・編集可 |
| `situation` | `string` | ✓ | ✓ | 状況。空不可 |
| `thought` | `string` | - | ✓ | 考え |
| `action` | `string` | - | ✓ | 行動 |
| `body` | `string` | - | ✓ | 体（どこがどんな感じか） |
| `intensity` | `number \| null` | - | ✓ | 強さ 0–100。未設定は `null`（`0` と区別する） |
| `emotions` | `string[]` | - | ✓ | 感情の名前。プリセット＋自由入力、複数可 |
| `note` | `string` | - | **✗** | 備考。**印刷対象外** |
| `printedAt` | `number \| null` | ✓ | - | 印刷操作をした時刻(epoch ms)。`null`=未印刷 |
| `createdAt` | `number` | ✓ | - | 自動・epoch ms |
| `updatedAt` | `number` | ✓ | - | 自動・epoch ms |

- 保存に必須なのは `date`（初期値あり）と `situation`（空文字不可）のみ。他は空のまま保存可。
- optional 項目の入力を強制しない（つらい瞬間の記録負担を下げる方針）。

### EmotionPreset（感情プリセット）

- ユーザーが編集（追加・削除・改名）できる感情語の一覧。初期値は同梱の叩き台。
- 記入時に自由入力した感情語は **そのレコード限り**。プリセットへ自動昇格はしない（誤操作防止）。

### IndexedDB ストア構成

- DB 名：仮 `kokoro-log`、version 1。
- `entries`：keyPath `id`。`date` にインデックス（将来の月絞り込み用）。
- `emotionPresets`：プリセット語の一覧。

## 画面

| ルート | 役割 |
|---|---|
| `/` | 一覧。出来事日時の**降順**。各行＝日時＋状況（長ければ省略）＋（感情/強さを小さく添える）＋**印刷済みチェック**。検索/フィルタは v1 では無し |
| `/entry/new` | 新規記入 |
| `/entry/:id` | 詳細・編集・**削除**・印刷・印刷済みの手動トグル |
| `/settings` | 感情プリセット管理（追加/削除/改名。並べ替えは後回し） |

### 記入/詳細画面

- 常時表示（デフォルトで入力可能）：日時（初期値 now）・状況・考え・行動。
- `<details>/<summary>` で初期収納：体・強さ（スライダー＋数値、未設定可）・感情（チップ選択＋自由入力）・備考。
- 保存必須：日時・状況のみ。
- 未保存の変更がある状態での離脱時は警告（アプリ内遷移ガード＋`beforeunload`）。ドラフト自動保存は v1 では実装しない。

### 削除

- 確認モーダル → 実行後に「元に戻す」Undo トースト（確定まで一時保持）。

## 印刷

- 詳細画面の「印刷」→ `window.print()` + `@media print`。**1件＝A4縦1ページ**。
- 出力順：日時（見出し）→ 状況 → 考え → 行動 → 体 → 強さ → 感情の名前。
- 未入力の optional 項目は**見出しごと省略**。**強さは `n / 100`** の数値表記。**備考は印刷しない**。
- 装飾は最小（白地に黒・読みやすい本文サイズ）。アプリ名等のブランディングは入れない。
- `afterprint` で `printedAt` をセット。「印刷したか」は厳密には検知不能なため、意味は「**印刷操作をした**」であり、詳細画面で手動補正できる。
- v1 はまず1件印刷を確認。複数選択の一括印刷（各件1ページ）は後回し。

## PWA

- 更新方式：`registerType: 'prompt'`（書きかけ保護のため自動更新しない）。
- manifest：`id: '/'` を固定し将来のリネームでも同一アプリ性を維持。`name`/`short_name` は**仮名**（後で変更可。IndexedDB はオリジン単位なのでリネームでデータは失われない）。`display: standalone`、テーマ色、アイコン整備。

## v1 スコープ外（将来）

- エクスポート/インポート、バックアップ
- 端末間同期
- 一覧の検索・日付/感情/強さフィルタ、月見出しグルーピング
- 複数選択の一括印刷
- ドラフト自動保存・復元
- プリセットの並べ替え、自由入力語のワンタップ昇格
