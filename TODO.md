# 残課題 / TODO

v1 リリース（Cloudflare Pages デプロイ・実機確認済み）時点で先送りした事項のメモ。
各項目に「なぜ」「どこに手を入れるか」を添えておく。仕様の確定経緯は [`SPEC.md`](./SPEC.md) 参照。

優先度の目安: 🔴 高 / 🟡 中 / ⚪ 低

---

## 🔴 バックアップ（エクスポート / インポート）

- **背景**: データは端末内 IndexedDB のみ。ブラウザのサイトデータ消去・iOS のストレージ削除・
  端末故障で**全データが消える**。現状その保険が無い。データモデルは JSON で完結するよう設計済み
  なので、出力自体は比較的低コスト。
- **やること**:
  - 全 `entries` ＋ `emotionPresets` を JSON でエクスポート（ダウンロード）。
  - エクスポート時に端末タイムゾーンを付与する案（[`SPEC.md`] のデータモデル節参照）。
  - JSON インポート（マージ or 置換のポリシー要検討。`id` 衝突時の扱い）。
- **入り口**: `src/db/` に export/import 関数を追加し、設定画面 `SettingsView` に導線。

## 🟡 印刷レイアウトの調整

- **背景**: 実利用で「印刷は調整が要る」と判明（担当医に渡す体裁の最適化）。
- **やること**: `src/routes/PrintSheet.tsx` と `src/app.css` の `@media print` を調整。
  余白・フォントサイズ・項目間隔・改ページ（`break-inside`）など。実際の印刷結果を見ながら詰める。

## 🟡 一覧の検索 / フィルタ

- **背景**: v1 は検索なし。件数増加に備え、最低でも日付（月単位）絞り込みを後付けできるよう
  `entries` に `by-date` インデックスを張ってある。
- **やること**:
  - 月単位の絞り込み（インデックス活用 or 全件ロード後 JS フィルタ）。
  - 感情・強さでの絞り込み、テキスト検索。
  - 月区切りの見出しグルーピング。
- **判断材料**: 複合フィルタを本格化するなら `idb` → `Dexie.js` 移行も選択肢（経緯は会話 / SPEC 参照）。
  データアクセスは `src/db/` に隔離済みなので差し替えは局所的。

## 🟡 複数選択の一括印刷

- **背景**: v1 は1件印刷のみ確認済み。複数選択して各件1ページで一括印刷したい。
- **やること**: 一覧に選択UI、選択分の `PrintSheet` を並べて改ページ（`@page` / `break-after: page`）。

## ⚪ 記入中ドラフトの自動保存・復元

- **背景**: 離脱警告のみ（`beforeunload` ＋ ボタン経由の confirm ＋ 戻るボタンの離脱ガード）。
  自動下書き保存は未実装。
- **やること**: 入力内容を一時保存（`sessionStorage` or 一時 IndexedDB キー）し、誤って閉じても復元。

## ⚪ 感情プリセットの改善

- **背景**: v1 は追加 / 削除 / 改名のみ（改名は input の `onBlur` 反映）。
- **やること**:
  - 並べ替え（`order` フィールドは用意済み。ドラッグ等のUI）。
  - 「最近自由入力した感情」をワンタップでプリセット化する導線（※誤操作源になり得るので慎重に。
    自由入力の自動昇格はしない方針は維持）。

## ⚪ 将来構想：端末間同期

- 現状は「単一端末・同期なし」が確定方針。もし複数端末で使いたくなった場合は
  Cloudflare D1 + 認証（Pages Functions）等が必要で、アーキテクチャの大きな変更になる。
